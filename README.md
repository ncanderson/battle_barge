# Battle Barge

## Dependencies

This application uses `pyproject.toml` to manage dependencies

## Setup

This repository provides `build_venv.sh`, which will create a local `.venv` for development, using the dependencies specified in the `pyproject.toml` file. To activate the `.venv`, use the following:

```bash
source ./.venv/bin/activate
```
