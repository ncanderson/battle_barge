# Standard imports
from enum import Enum, auto

# 3rd party imports

# Module imports

################################################################################

class Difficulty(Enum):
    EASY = auto()
    MEDIUM = auto()
    HARD = auto()

################################################################################
